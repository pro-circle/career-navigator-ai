# --- Server-side (required for AI features) ---
# Groq API key. Get one at https://console.groq.com
GROQ_API_KEY=

# --- Client-side Supabase (optional; leave blank to disable persistence) ---
# Safe to expose: anon key + project URL are meant for the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
